{
  "name": "tally",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:generate": "pnpm --filter @tally/db db:generate",
    "db:migrate": "pnpm --filter @tally/db db:migrate",
    "db:deploy": "pnpm --filter @tally/db db:deploy",
    "db:seed": "pnpm --filter @tally/db db:seed",
    "db:studio": "pnpm --filter @tally/db db:studio",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "turbo clean && rm -rf .turbo node_modules",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "@tally/config": "workspace:*",
    "dotenv": "^17.4.2",
    "eslint": "^9.30.0",
    "globals": "^16.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.5",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.1"
  }
}
